name: Code Quality

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  code-quality:
    name: Code Quality Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run ESLint with detailed output
        run: |
          pnpm run lint -- --format=json --output-file=eslint-report.json || true
          pnpm run lint

      - name: Run Prettier with detailed output
        run: |
          pnpm exec prettier --check --write "**/*.{js,jsx,ts,tsx,json,md}" || true

      - name: TypeScript strict check
        run: |
          pnpm run type-check -- --strict

      - name: Check for unused dependencies
        run: |
          pnpm exec depcheck --json --ignores="eslint,prettier,husky,lint-staged" || true

      - name: Bundle size analysis
        run: |
          pnpm run build
          if [ -f ".next/static/chunks/pages/_app-*.js" ]; then
            echo "Bundle size analysis:"
            find .next/static/chunks -name "*.js" -exec ls -lh {} \; | head -10
          fi

      - name: Upload ESLint report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: eslint-report
          path: eslint-report.json
